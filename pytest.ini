[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    slow: walks the whole migration chain in a subprocess
filterwarnings =
    ignore::DeprecationWarning
