# Nova Cinema — состояние проекта

> Живой документ. Обновляется после каждого завершённого этапа.
> Последнее обновление: **этап 20 завершён — все этапы ТЗ пройдены**.

---

## 1. Кратко

Telegram Mini App кинотеатра Nova Cinema: React-фронт внутри Telegram WebApp, FastAPI-бэкенд,
отдельный aiogram-бот, PostgreSQL в проде и SQLite в разработке.

Онлайн-оплаты нет и не будет: пользователь создаёт **заявку**, администратор связывается с ним
и подтверждает бронь вручную через админ-панель.

**Главный принцип проекта:** ничего, что видит зритель, не зашито в код. Фильмы, цена, расписание,
новинки, бонусы, мелодии, контакты, размер зала и правила бронирования живут в базе и меняются
администратором без правки исходников и без обращения к разработчику.

| Показатель | Значение |
|---|---|
| Backend | 3 350 строк Python в `backend/` |
| Frontend | 5 809 строк TS/TSX |
| Тесты | **211 тестов** |
| API | **59 маршрутов** |
| Таблиц в БД | **13** |
| Миграций | 14, head = `0014_timezone_and_one_review`, цепочка проходит вверх и вниз |
| Словари RU/UZ | по 420 ключей, синхронны |

---

## 2. Архитектура

```
nova-tg-only/
├── app.py                      тонкий шим: uvicorn app:app, Dockerfile, VS Code launch
├── bot.py                      aiogram-бот (не импортирует веб-слой)
├── seed_movies.py              CLI-сидер демо-каталога
├── backend/
│   ├── core/
│   │   ├── config.py           единственное место чтения окружения + нормализация
│   │   ├── checks.py           предупреждения о конфигурации при старте
│   │   ├── db.py               engine, SessionLocal, Base, get_db, utcnow
│   │   └── security.py         валидация Telegram initData, JWT, зависимости доступа
│   ├── models/                 user · movie · show · booking · settings
│   ├── schemas/                auth · movie · booking · show · profile · settings
│   ├── services/               catalog · settings · i18n · pricing · hall · deep_link
│   │                           booking · telegram · tmdb · media · seed
│   ├── api/
│   │   ├── deps.py             один импорт для роутеров
│   │   └── routers/            auth · public · catalog · booking · profile
│   │                           admin · admin_catalog · admin_content
│   └── main.py                 сборка приложения: CORS, статика, lifespan, роутеры
├── alembic/versions/           15 миграций, линейная цепочка
├── tests/                      pytest + httpx.AsyncClient поверх SQLite
└── frontend/src/
    ├── api/                    client · types · settings · catalog · booking · profile · admin
    ├── components/             Shell · BottomNav · MovieCard · LanguageSwitcher
    │                           TelegramRouteControls · DeepLinkHandler · ProtectedAdminRoute
    ├── i18n/                   index (рантайм) · ru · uz
    ├── lib/                    haptic · hall · dates · deepLink · telegramTheme
    ├── pages/                  Home · MoviePage · About
    │   ├── booking/            DatePage · TimePage · HallPage · Success
    │   ├── admin/              Admin · AdminMovies · AdminMovieForm · AdminNewReleases
    │   │                       AdminSessions · AdminPrice · AdminBonuses · AdminMelodies
    │   │                       AdminCinemaSettings · AdminGallery · AdminReviews
    │   │                       moviePayload
    │   └── profile/            Profile
    └── App.tsx                 только роутинг
```

**Слои бэкенда:** роутер принимает и валидирует → сервис содержит бизнес-правило → модель работает
с БД. Роутеры не лезут во внешние API напрямую, сервисы не знают про HTTP-статусы кроме явных
`HTTPException` в местах, где правило домена совпадает с ответом API.

---

## 3. Модель данных

| Таблица | Назначение |
|---|---|
| `users` | аккаунт Telegram, роль (`user`/`admin`) |
| `movies` | каталог, i18n-поля RU/UZ, `is_new` + `new_until` |
| `reviews` | отзывы, поле `approved` для модерации |
| `shows` | **расписание — единственный источник правды** |
| `bookings` | заявки, `ticket_price` заморожен, `created_at`, `completed_at` |
| `seat_holds` | временная блокировка мест |
| `favorites` | избранное |
| `user_notifications` | уведомления зрителю |
| `admin_notifications` | уведомления администратору |
| `cinema_settings` | **одна строка (id=1) — весь профиль кинотеатра** |
| `bonuses` | акции и бонусы |
| `melodies` | мелодии, максимум 3 |
| `gallery_images` | фото интерьера/зала/бара |

### Что содержит `cinema_settings`

Название, адрес, телефон, Telegram, Instagram, username бота, ссылка на карту, широта/долгота, часы работы,
описание — всё с UZ-вариантами. Плюс: цена билета, валюта, `hall_rows` × `hall_cols`,
`max_seats_per_booking`, `hold_minutes`, `booking_days_ahead`.

### Жизненный цикл брони

```
pending ──► contacting ──► confirmed ──► watched
   └────────────┴─────────────┴──► cancelled
```

- Отмена освобождает места, **все остальные статусы их держат**.
- QR действителен для `confirmed` и `watched`.
- Отзыв доступен при статусе `watched` **и** только после окончания сеанса.

---

## 4. Выполненные этапы

### Этап 1 — аудит ✅

Разобрана архитектура, найдены мёртвый код, дублирование, устаревшие зависимости и баги.
Составлен план этапов 2–20 и выбран путь реализации.

### Этап 2 — фундамент и архитектура ✅

- Монолит `app.py` (882 строки) разобран на пакет `backend/` со слоями. `app.py` остался шимом,
  поэтому `uvicorn app:app`, Dockerfile и VS Code launch работают без изменений.
- `bot.py`, `seed_movies.py`, `alembic/env.py` отвязаны от веб-слоя.
- Фронтенд разложен на `api/`, `components/`, `i18n/`, `lib/`, `pages/`. Монолиты `api.ts` (237)
  и `i18n.ts` (662) разделены; импорты вида `from './api'` продолжают работать через index-файлы.
- Удалён мёртвый код: `web/` (vanilla-JS прототип), `nova.db` (легаси-SQLite).
- Поднят pytest, написаны смоук-тесты критичного пути.

**Починенные баги:**

| Баг | Суть |
|---|---|
| Приложение не стартовало локально | `DATABASE_URL=` пустой → `create_async_engine("")` падал при импорте |
| CORS не совпадал | у origin в конфиге был хвостовой слэш, браузер его не шлёт |

### Этап 3 — база данных и центральная система настроек ✅

- **Настройки**: `cinema_settings` расширена с 2 до 26 полей. `GET /api/settings` (публичный),
  `GET`/`PUT /api/admin/settings`.
  **Поправка к прежней версии этого отчёта:** здесь было сказано, что контакты в `bot.py`
  перестали быть хардкодом. Это было неверно — API настроек сделали, а бота к нему не
  подключили. Фактически исправлено на этапе 8.
- **Расписание**: таблица `shows` заменила связку `Movie.sessions_json` + `session_prices`.
  Раньше `ensure_public_session()` принимал **любое** время формата `HH:MM` — теперь бронировать
  можно только реальный активный сеанс. Добавлен `POST /api/admin/sessions/bulk`.
- **Историческая цена**: `Booking.ticket_price` фиксируется в момент заявки. Изменение цены
  админом действует только вперёд.
- **Статусы**: добавлены `contacting` и `watched`, `completed` мигрирован в `watched`,
  `completed_at` наконец заполняется.
- **Зал**: геометрия переехала в настройки. Структурно устранён баг, где `seats_count` по умолчанию
  был 80 в БД против 15 в API — количество мест больше не хранится на сеансе.
- **Новинки, бонусы, мелодии, галерея**: модели, CRUD и публичные эндпоинты.
- **`Booking.created_at`** — колонки не было, а ТЗ 4.8 требует «время создания».
- Из этапа 9 перенесён **выбор сеанса**: экран времени больше не просит ввести время вручную,
  а показывает реальный список из расписания; окно дат берётся из `booking_days_ahead`.

**Миграция `0012`** проверена на живой БД в обе стороны: `session_prices` копируется 1:1, времена из
`sessions_json` разворачиваются на 14 дней вперёд, `completed` → `watched`, `ticket_price`
восстанавливается из `total`. Downgrade возвращает `session_prices` из `shows`.

**Попутно починен предсуществующий блокер:** миграция `0010` использовала Postgres-only
`ALTER COLUMN ... TYPE BIGINT` и роняла всю цепочку на SQLite — локальная разработка и тесты
миграций были невозможны. Добавлена проверка диалекта, на Postgres поведение не изменилось.

### Этап 4 — полноценная админ-панель ✅

Одиннадцать разделов вместо шести. Бэкенд для большинства был готов с этапа 3 — добавилась
только модерация отзывов.

| Раздел ТЗ | Что сделано |
|---|---|
| 4.1 Dashboard | десять карточек: новые заявки, все пять статусов, фильмы, сеансы, всего заявок, доход |
| 4.2 Фильмы | было; добавлены переключатели новинки в форме |
| 4.3 Новинки | новый раздел: пометить, снять, задать дату окончания, фильтр «только новинки» |
| 4.4 Сеансы | форма переведена на `start_time`; добавлен режим «Диапазон дат» поверх `POST /sessions/bulk` |
| 4.5 Цена | отдельный раздел: текущая цена, валюта, статус, поле новой цены |
| 4.6 Бонусы | новый раздел: CRUD, RU/UZ, включение/выключение, порядок |
| 4.7 Мелодии | новый раздел: загрузка аудио, переименование, замена файла, счётчик 3 |
| 4.8 Брони | добавлены Telegram ID, время создания, количество мест, замороженная цена и «Начать обработку» |
| 4.9 Настройки | новый раздел на пять блоков: инфо, контакты, описание, зал, правила бронирования |
| — | модерация отзывов: `approved` наконец получил интерфейс |

**Новые эндпоинты:** `GET/PATCH/DELETE /api/admin/reviews`.

**Попутные улучшения:** `toMoviePayload`/`emptyMoviePayload` вынесены в общий модуль вместо дубля;
статусы `contacting` и `watched` добавлены во все фильтры и селекты; `AdminSessionPayload` заменил
`Omit<AdminSession, …>`, который тянул за собой легаси-поле `session`.

### Этап 5 — Telegram Mini App: основной интерфейс ✅

Главная страница собрана из всех элементов, которые перечисляет ТЗ.

| Элемент | Состояние |
|---|---|
| Каталог | был; вынесен в переиспользуемый `MovieCard` с бейджем «Новинка» |
| Поиск | был |
| Жанры | был |
| **Новинки** | новая горизонтальная лента, скрывается при выборе конкретной даты |
| **Доступные даты** | новая лента чипов; окно берётся из `booking_days_ahead`, не из константы |
| **Профиль** | новая нижняя навигация: Афиша · Билеты · Профиль (+ Админ для админов) |
| AI-помощник | исключён из ТЗ владельцем |

**Фильтр по дате.** `GET /api/movies` получил параметр `date`: каталог сужается до фильмов
с активным сеансом в этот день, а `sessions` содержит времена только этого дня. Раньше поле
объединяло времена всех дат.

**Нижняя навигация.** В CSS с самого начала было зарезервировано `padding-bottom: 94px` под таб-бар,
которого не существовало — место просто пустовало. Теперь там навигация, а дублирующая ссылка
на профиль из шапки убрана.

**Валюта перестала быть захардкоженной.** С этапа 3 админ может её менять, но `formatMoney`
всё ещё брал значение из словаря. Добавлен `setCurrency()`: значение приходит из
`GET /api/settings` и из ответа эндпоинта мест, словарь остался фолбэком.

**Узбекские даты.** Chrome не имеет коротких названий месяцев для латинского узбекского —
`Intl` выдавал «M08 22, Sat». Названия прописаны явно, теперь «Shan, 22 Avg».

**Демо-сид получил UZ-поля.** Фильмы из админки переводятся автоматически, а сид заполнял только
русские колонки, поэтому в UZ-режиме каталог оставался русским и проверить двуязычность на демо-данных
было нельзя.

**Проверено в браузере** на мобильном вьюпорте 375×812: обе страницы, переключение RU/UZ с сохранением
после перезагрузки, фильтр по датам, лента новинок, нижняя навигация. Ошибок в консоли нет.

### Этап 6 — каталог и карточки фильмов ✅

Карточка получила два недостающих элемента из перечня ТЗ; остальное было с прошлых этапов.

**Доступные сеансы в карточке.** `GET /api/movies/{id}` отдаёт новое поле `schedule` —
сеансы, сгруппированные по датам. Прошедшие дни отбрасываются, окно совпадает с
`booking_days_ahead`, неактивные сеансы не показываются: карточка не может предложить слот,
который нельзя забронировать. Тап по времени ведёт сразу в зал, минуя выбор даты и времени.

**Кнопка «Поделиться».** Формирует Telegram-ссылку `t.me/<bot>?startapp=movie_<id>`.
Username бота добавлен в настройки кинотеатра (миграция `0013`) — админ задаёт его сам,
в публичном ответе `@` срезается. Внутри Telegram открывается системный диалог «Поделиться»,
вне Telegram ссылка копируется в буфер. Приём такой ссылки и обработка битых/устаревших —
этап 8.

**Проверено в браузере:** расписание на 7 дней с переходом в зал по тапу, кнопка выдаёт
корректную ссылку, зал открывается (3 ряда, 15 мест), ошибок в консоли нет.
Миграция `0013` прогнана вверх и вниз.

### Этап 7 — новинки и поиск ✅

**Поиск расширен** с одного названия до всех данных карточки: название и его часть, жанр,
описание, страна, режиссёр, актёры и год. `GET /api/movies` получил параметр `q`.

Сопоставление выполняется в Python, а не в SQL, и это осознанно: `LIKE` и `lower()` в SQLite
сворачивают регистр только для ASCII, поэтому «дюна» не нашла бы «Дюна» на локальной базе,
хотя на PostgreSQL всё работало бы. Каталог одного зала невелик, цена такого решения — ноль.

**Новинки стали фильтром.** Параметр `only_new` и одноимённый чип на главной. Раньше новинки
были только лентой; теперь по ним можно отфильтровать каталог. Флаг учитывает `new_until`,
то есть истёкшие новинки в выдачу не попадают.

**Поиск ушёл на сервер** с дебаунсом 300 мс, жанр остался клиентской фасеткой поверх
результата. Пустая выдача показывает «Ничего не найдено» и кнопку сброса всех фильтров.

**Найден и исправлен баг с актёрским составом.** `localized_movie_text` брал
`cast_json_ru or cast_json`, но дефолт локализованных колонок — строка `"[]"`, которая
истинна. Из-за этого у всех фильмов, созданных не через админскую форму (демо-сид, прямой
insert, импорт), поле «В ролях» на карточке было пустым — что и было видно на этапе 6,
но я тогда прошёл мимо. Добавлен `_cast()` с проверкой на пустой список и регрессионный тест.

### Этап 8 — «Поделиться» и deep links ✅

Кнопка появилась на этапе 6, здесь сделана приёмная сторона.

**Формат ссылки вынесен в один модуль** `services/deep_link.py`. Его используют все трое:
эндпоинт, который отдаёт `share_link` в карточке, бот, который принимает `/start`, и Mini App,
которая читает `start_param`. Разъехаться они больше не могут. Фронтенд перестал собирать
ссылку сам — берёт готовую из ответа API.

**Бот** получил обработчик `CommandStart(deep_link=True)`, зарегистрированный **до** обычного
`/start`, иначе тот перехватывал бы всё. Показывает карточку фильма, а на битую ссылку и на
недоступный фильм отвечает понятным текстом и меню. Заодно устранён дубль отрисовки карточки —
`send_movie_card` теперь общий для deep link и каталога бота.

**Mini App** читает параметр запуска из четырёх источников. Причина: SDK переписывает URL сразу
после старта и вычищает `tgWebApp*`, что может произойти раньше монтирования React — поэтому
кроме хэша читается и `sessionStorage.__telegram__initParams`, куда SDK всё складывает.
Переход делается с `replace: true`, чтобы кнопка «Назад» вела в каталог, а не в пустоту.

**Три случая из ТЗ обработаны и проверены в браузере:**

| Случай | Поведение |
|---|---|
| Удалённый или снятый с публикации фильм | экран «Фильм недоступен» с кнопкой в каталог |
| Старая ссылка | тот же экран — для пользователя это одно и то же |
| Неправильная ссылка | параметр игнорируется, открывается каталог |

Вместо сырого английского `Movie not found` из API теперь локализованный экран: для этого
`ApiError` научился нести HTTP-статус, и карточка отличает 404 от сетевой ошибки.

**Починена нестабильность тестов.** Набор падал при повторном запуске: движок SQLAlchemy
переживал смену event loop, и соединения из пула, созданные в предыдущем цикле, ломали
следующие тесты. Теперь движок закрывается на teardown каждого теста. Три полных прогона
подряд — 108 тестов зелёные каждый раз.

### Этап 9 — бронирование ✅

Пройден как ревизия: путь был реализован на этапах 3–6, задача была проверить каждый пункт
ТЗ и закрыть найденное. Проверял тестами, а не чтением — так нашлось то, чего глазами
не видно.

**Подтверждено соответствие ТЗ:**

- один зал, категорий мест (VIP/Standard/Premium) нет нигде в коде и в контракте API;
- путь фильм → дата → сеанс → места → цена → заявка проходится целиком;
- сумма = места × актуальная цена из админки;
- **клиент не может продиктовать цену** — подделанный payload с `ticket_price: 1`
  игнорируется, сервер считает сам. Отдельный тест на это.

**Найдено и исправлено:**

| Дефект | Суть |
|---|---|
| Бронь на прошедшую дату | интерфейс такие даты прятал, но API их принимал — сеанс вчерашнего дня бронировался прямым запросом. Проверка перенесена в `ensure_bookable_slot`, то есть закрыты сразу все три эндпоинта бронирования |
| Цена не видна до выбора мест | ТЗ выделяет «видит актуальную цену» отдельным шагом, а в сводке до выбора стояло «0 UZS». Добавлена плашка с ценой за билет и лимитом мест |
| Экран дат вёл в тупик | предлагались все даты окна бронирования, включая те, где фильм не идёт; на следующем шаге пользователь упирался в «сеансов нет». Теперь даты берутся из расписания фильма, у каждой видно число сеансов и первые времена |
| «4 сеансов» | неверная форма множественного числа. Добавлен `formatSessionCount` на `Intl.PluralRules` с тремя русскими формами |

**Проверено вживую**, а не только тестами: поднят стенд, подложен реальный JWT, пройден весь
путь до конца — выбраны места A1 и A2, сумма 60 000 UZS, заявка создана со статусом `pending`,
`ticket_price` заморожен, места помечены занятыми. Ошибок в консоли нет.

### Этап 10 — схема зала и защита от двойного бронирования ✅

**Четыре статуса места вместо двух.** `GET /api/sessions/{id}/seats` отдавал одно поле `taken`,
куда были слиты и брони, и чужие удержания. Теперь раздельно: `booked` (подтверждено или
просмотрено), `awaiting` (заявка ждёт решения админа либо кто-то занимает место прямо сейчас),
`mine` (мои собственные удержания), и `taken` как объединение для простых проверок.
Эндпоинт получил необязательную авторизацию — иначе собственное удержание выглядело бы
для самого зрителя как занятое чужим.

**Защита от гонки.** Проверка «место свободно» и вставка шли двумя запросами, а
`.with_for_update()` на SQLite не блокирует ничего. Теперь арбитром выступает уникальный
индекс `uq_seat_hold`: при конфликте `IntegrityError` превращается в честный 409 вместо 500.
Тест запускает два удержания одного места через `asyncio.gather` и требует ровно один 200,
ровно один 409 и одну выжившую запись.

**Удержание наконец удерживает.** Экран зала вызывал `holdSeats` и `confirmBooking` подряд,
поэтому настройка `hold_minutes` не влияла ни на что. Теперь удержание берётся при выборе
мест (с дебаунсом), на экране идёт обратный отсчёт, при снятии выбора место освобождается
через новый `DELETE /api/holds`, а при истечении выбор сбрасывается. Перезагрузка страницы
восстанавливает выбор из `mine`.

**Найден и исправлен баг в `get_settings`.** Функция создавала строку настроек по схеме
«проверил — вставил», а вызывается почти из каждого эндпоинта. Два параллельных запроса к
свежей базе оба видели пустоту, оба вставляли `id=1`, и проигравший падал с 500. Первая
попытка починить это внутри той же сессии дала `MissingGreenlet` — сломанная транзакция
не чинится изнутри себя. Правильное решение: создавать строку в отдельной сессии, тогда
проигрыш в гонке не портит транзакцию запроса.

**Проверено вживую:** зал с местами во всех четырёх состояниях, легенда, блокировка занятых
кнопок, таймер удержания 9:59 из настройки в 10 минут, освобождение места на сервере при
снятии выбора.

### Этапы 11–12 — обработка заявок и статусы ✅

Пройдены как ревизия, по образцу этапа 9: пункты ТЗ проверены тестами, найденное закрыто.

**Подтверждено соответствие ТЗ:**

- платёжных интеграций нет нигде — ни Payme, ни Click, ни каких-либо шлюзов, ни в коде,
  ни в зависимостях; в ответе на бронирование нет полей, похожих на оплату;
- бронирование создаёт **заявку** со статусом `pending`, она попадает в админ-панель
  с телефоном, именем, Telegram-ником, прямой ссылкой на чат и комментарием — всем,
  что нужно, чтобы перезвонить и решить оплату вне приложения;
- админ подтверждает и отменяет, зритель получает уведомление с причиной отказа;
- статусы ровно пять, посторонние отклоняются с 422;
- отмена освобождает места, подтверждение закрепляет;
- `completed_at` ставится один раз и не переписывается повторным переводом в `watched`.

**Найдены и исправлены две дыры одного класса** — смена статуса трактовалась как простое
переключение флага, хотя места за это время могли уйти другому:

| Дефект | Сценарий |
|---|---|
| Оживление отменённой заявки | A бронирует место, админ отменяет, место освобождается, B его занимает, админ передумывает и возвращает заявку A в `confirmed` — **две живые брони на одном месте**. Теперь такой переход отклоняется с 409 |
| Принятие предложенного времени | админ предлагает другое время, зритель соглашается — бронь переезжала на новое время **без проверки**, существует ли там сеанс и свободны ли места. Теперь несуществующий сеанс даёт 404, занятое место — 409, а предложение остаётся в силе |

Обе проверки опираются на общий `seats_taken_by_others()` в сервисе бронирования.

### Этап 14 — отзывы ✅

**Часовой пояс кинотеатра — закрыт открытый вопрос с этапа 9.** Сервер считает в UTC,
кинотеатр живёт в UTC+5, поэтому «сеанс закончился» без часового пояса ответить нельзя.
Добавлено поле `timezone_offset_minutes` в настройки (миграция `0014`, по умолчанию 300).
Смещение в минутах, а не имя зоны IANA: `zoneinfo` на машине без `tzdata`, а в Узбекистане
нет перевода часов — фиксированное смещение там точно и не тянет зависимость.

**Правило доступа приведено к ТЗ.** Раньше отзыв открывался по одному лишь статусу
`watched`, который администратор ставит вручную — то есть отзыв можно было оставить к фильму,
который ещё не начался. Теперь проверяется, что сеанс **действительно закончился**:
время начала плюс длительность фильма, в местном времени кинотеатра.

**Один отзыв на фильм от одного зрителя.** Ограничения не было вовсе — можно было отправить
сколько угодно отзывов и накрутить рейтинг, который считается как среднее. Добавлен
уникальный индекс `uq_review_per_movie`; миграция схлопывает существующие дубли, оставляя
новейший. Повторная отправка отвечает 409.

**Форма отзыва больше не висит всегда.** Карточка фильма отдаёт `can_review` и `has_reviewed`
(для этого `GET /api/movies/{id}` получил необязательную авторизацию), и Mini App показывает
либо форму, либо понятное объяснение, почему её нет.

**Обновлены три теста прошлых этапов**, которые кодировали прежнее правило: они помечали
`watched` на завтрашний сеанс и ждали, что отзыв откроется. Общий помощник
`watched_booking_in_the_past()` переехал в `conftest.py`.

### Этап 16 — раздел «О Nova Cinema» ✅

Бэкенд был готов с этапов 3–4, а в Mini App раздела не существовало вовсе — как и управления
галереей в админке, которое я тогда отложил именно сюда.

**Публичный экран `/about`.** Название, описание, адрес и часы работы — из настроек.
Кликабельный телефон (`tel:`), кнопки Telegram, Instagram и «Открыть на карте». Ссылка на
карту берётся из `map_url`, если админ его задал; иначе строится из координат, иначе из
адреса — то есть кнопка работает при любом уровне заполненности. Внутри Telegram ссылки
открываются через `openTelegramLink`/`openLink`, вне его — обычным окном.

**Галерея.** Интерьер, зал, барная зона — с подписями на обоих языках, порядок задаёт админ.
Цены и заказы бара не добавлены, как прямо требует ТЗ; отдельный тест проверяет, что таких
полей нет в контракте.

**Админ-раздел «Галерея».** Загрузка файла или ссылка, подписи RU/UZ, порядок, превью,
редактирование и удаление. Двенадцатая вкладка админки.

**Навигация.** В нижнюю панель добавлен пункт «О нас» — четыре вкладки у зрителя, пять у
администратора.

**Проверено в браузере:** обе языковые версии, три фотографии с подписями, `tel:`-ссылка,
активная вкладка навигации. Ошибок в консоли нет.

Фотографии интерьера пока не предоставлены — на стенде использовались временные картинки,
в проекте галерея пуста и показывает «Фотографии скоро появятся».

### Этап 17 — Telegram Web App ✅

Пять пунктов из шести уже работали; проверил их тестами, а не на глаз, и закрыл шестой.

**Доверие к клиенту проверено — дефектов нет.** 11 тестов на то, что сервер не верит
фронтенду: подпись `initData` обязательна, подделка идентификатора её ломает, подпись чужим
токеном бота отклоняется, просроченный `auth_date` не принимается, **JWT с ролью `admin`
не даёт админских прав** — роль пересчитывается из `ADMIN_TELEGRAM_IDS`, а не читается из
токена, и токен, подписанный другим секретом, отвергается.

**Тема Telegram — то, чего не было.** В CSS использовались лишь две переменные
`--tg-theme-*`, а `themeParams`, `colorScheme` и `setHeaderColor` не вызывались нигде,
поэтому шапка и фон клиента не совпадали с приложением. Добавлен `lib/telegramTheme.ts`:
параметры темы переносятся в CSS-переменные, шапка и фон сообщаются Telegram, изменение темы
на лету отслеживается через `themeChanged`. Вызовы обёрнуты — старые клиенты бросают
исключение на неизвестных методах, а тема не стоит падения приложения.

**Палитра осталась тёмной сознательно.** Перекрашивать кинотеатр в белое из-за светлой темы
телефона — значит воевать с брендом; владелец этого не просил. За Telegram следует
*обрамление* и акцент ссылок. На `<html>` пишется `data-color-scheme`, так что светлую
палитру можно добавить одним CSS-блоком, не трогая компоненты.

**Подтверждение закрытия.** Пока места удерживаются, закрытие Mini App молча их отдавало.
Теперь на это время включается `enableClosingConfirmation()`.

### Этап 18 — безопасность и надёжность ✅

Весь чек-лист ТЗ переписан в исполняемые проверки — 21 тест, отдельным файлом.

**Проверено, дефектов нет:** чужая бронь невидима и неуправляема (404 и на чтение, и на
принятие предложенного времени), чужие уведомления недоступны, публичные отзывы не выдают
автора, каждый админский маршрут перечисляется автоматически и проверяется на 403 для
зрителя (то самое «не только скрытием кнопок»), строки SQL-инъекций просто ничего не находят
и сохраняются как текст, ключи API и секреты не попадают ни в один ответ, `.env` вне git.

**Найдено и исправлено:**

| Дефект | Суть |
|---|---|
| 500 на слишком большом идентификаторе | `/api/movies/99999999999999999999` проходил валидацию, доходил до драйвера БД и падал с `OverflowError`. Любой мог уронить запрос строкой в адресе. Обработчик в `main.py` превращает это в 404 — сразу для всех маршрутов, нынешних и будущих |
| Размер загрузки сверялся после чтения | файл читался целиком, и только потом сравнивался с лимитом: сколько буферизовать, решал вызывающий. Теперь чтение идёт кусками и обрывается на превышении |

**Добавлены предупреждения о конфигурации при старте** (`backend/core/checks.py`). Ошибки
развёртывания тут молчаливые и дорогие: протухший CORS-origin выглядит как необъяснимо
пустой Mini App, а короткий `JWT_SECRET` не выглядит никак. Проверяются длина секрета,
наличие токена бота и списка админов, временные туннели в CORS и SQLite в проде. Ничего не
бросает — неверная настройка должна быть заметной, а не фатальной.

На текущем `.env` проверка сразу назвала три вещи, включая тот самый cloudflare-туннель,
который отмечен открытым вопросом с этапа 1.

**Остаточные риски**, сознательно не закрытые: нет ограничения частоты запросов и JWT живёт
12 часов без возможности отзыва. Для одного зала с ручной обработкой заявок это приемлемо;
`initData` подобрать нельзя, а админские маршруты закрыты списком Telegram ID.

### Этап 19 — полное тестирование ✅

**Сквозной сценарий.** Отдельные куски были покрыты 194 тестами, но не было ни одного,
который проходит цепочку ТЗ целиком и доказывает, что куски стыкуются. Написан
`test_end_to_end.py`: админ заводит фильм, расписание, цену и username бота → зритель
листает каталог, ищет по режиссёру, фильтрует новинки и по дате → открывает карточку с
расписанием и share-ссылкой → выбирает сеанс, места, видит цену → создаёт заявку → админ
берёт в работу и подтверждает → зритель видит бронь в истории с действующим QR → день
проходит, админ ставит «просмотрено» → зритель оставляет отзыв → админ его модерирует.
Плюс два ответвления: тот же путь на узбекском и отказ администратора с освобождением мест.

**Ручной прогон в браузере** на мобильном вьюпорте, оба языка: каталог, поиск, карточка,
расписание, зал с четырьмя статусами мест, таймер удержания, заявка, история, отзыв,
раздел «О нас», галерея.

**Найдено и исправлено:**

| Дефект | Суть |
|---|---|
| Валюта расходилась между экранами | в зале «30 000 UZS», в истории «60 000 сум». `setCurrency` вызывался только на главной и в зале, поэтому при прямом заходе на другой экран бралось значение из словаря, а не из настроек. Загрузка перенесена на уровень приложения — один вызов на все экраны |
| Контакты не связаны с профилем | зритель вводил имя и телефон при каждой брони заново, хотя в профиле для них есть поля. Теперь при первой заявке пустые поля профиля заполняются введённым, а форма брони подставляется из профиля. Существующие значения не перезаписываются — можно бронировать за друга |

**Подтверждено живьём:** дефект этапа 13 (статус брони показан как сырое `watched` вместо
подписи) действительно существует и описан в ТЗ, отправленном Codex. Не трогал — его этап.

### Этап 20 — финальная проверка и полировка ✅

Последний этап — не новая функциональность, а полный проход по проекту: запуск, зависимости,
импорты, миграции, мёртвый код, бэкенд, фронтенд, мобильный интерфейс и UX.

**Время кинотеатра.** Самое серьёзное из найденного. Сервер считал «сегодня» по собственной
дате, а Узбекистан живёт в UTC+5. Проверка велась только по дате, поэтому сеанс, который начался
(и закончился) несколько часов назад, всё ещё показывался в расписании и принимался к
бронированию — администратор получал заявку на сеанс, которого уже не было. Введены `cinema_now()`
и `session_has_started()`, и теперь по часам кинотеатра считаются: список дат бронирования,
расписание в карточке, список времён на дату, срок «новинки» и проверка при создании заявки.
Обнаружено на живом стенде: сервер стоял на 21 августа, кинотеатр уже жил 22-м.

**Цепочка миграций.** Откат ниже `0008` падал на `no such index: ix_session_prices_show_date`:
`0012` пересоздавала таблицу `session_prices`, но не её индексы, а ревизия ниже пыталась их
удалить. Для продакшена это означало, что откат обрывается на полуразобранной схеме. Индексы
восстановлены, цепочка проходит вверх, вниз до базы и снова вверх; на это написан тест.

**Контент без экрана.** Бонусы и мелодии администратор заполнял, публичные эндпоинты их
отдавали, а показать их было негде: ни один экран Mini App их не запрашивал. Подсказка в админке
при этом обещает «Включённые видят зрители». Оба блока добавлены в раздел «О нас».

| Найдено | Исправлено |
|---|---|
| Сеанс, который уже начался, предлагался и бронировался | расписание и заявки считаются по часам кинотеатра |
| Откат миграций падал ниже `0008` | `0012` восстанавливает индексы `session_prices` |
| Бонусы и мелодии некуда было показать | два блока в разделе «О нас», скрыты когда пусто |
| `.env.example` из README не существовал | создан; тест проверяет, что в нём есть каждая переменная |
| `bot.py` разбирал окружение вторым, более слабым парсером | читает через `backend/core/config.py`; `.env` больше не перебивает реальное окружение |
| Неизвестный адрес открывал пустой экран | экран «Страница не найдена» и catch-all маршруты |
| Ошибки сервера показывались зрителю по-английски | `apiMessage()` переводит закрытый набор ответов API |
| Описание кинотеатра рисовалось стилем надзаголовка: 12px, капсом | у надзаголовка свой класс |
| «Nova ★ 0» у фильма без оценок, пустой заголовок «В ролях» | пустое не показывается |
| Мёртвый код | 12 ключей словаря × 2 языка, `getBasePrice` вместе с его маршрутом, экспорт `getCurrentLanguage`, неиспользуемый импорт |
| README врал | `alembic.exe` (сломан путём внутри venv), не было команды запуска бота, устаревшая структура, не все переменные |

**Тесты этапа:** содержимое раздела «О нас», шаблон `.env.example`, конфигурация бота, цепочка
миграций в обе стороны, начавшийся сеанс. Плюс фикстура `cinema_clock`: расписание проверялось
фиксированным временем и зависело от часа запуска — теперь тесты о правиле, а не о часе.

**Ручная проверка** на мобильном вьюпорте, оба языка: главная, поиск, карточка, выбор даты и
сеанса, зал, «О нас» с бонусами и мелодиями, несуществующий адрес, профиль без авторизации.
Ошибок в консоли нет.

---

## 5. Ключевые технические решения

| Решение | Причина |
|---|---|
| `app.py` оставлен шимом, а не удалён | не ломать `uvicorn app:app`, Dockerfile CMD и launch.json |
| Одна таблица `shows` вместо `sessions_json` + `session_prices` | два источника правды делали расписание неуправляемым |
| Типизированные колонки настроек вместо key-value | валидация Pydantic 1:1 с формой админки |
| `Booking.ticket_price` отдельно от `total` | цена не должна переписывать историю задним числом (ТЗ 4.5) |
| Размер зала в настройках, а не на сеансе | зал один; на сеансе он порождал рассинхрон 80 против 15 |
| `ShowIn` принимает `session` как алиас `start_time` | админка этапа 2 ещё шлёт старое имя; алиас уйдёт в этапе 4 |
| Лимит мелодий проверяется в сервисе, а не в БД | понятная ошибка 409 вместо нарушения ограничения |
| `DEFAULT_*` только засевают строку настроек | после первого запуска авторитетна БД, не окружение |
| Мажорные апгрейды (React 19, Vite 8, Tailwind 4) отложены | риск не окупается в рамках этого ТЗ |
| Тесты на SQLite, прод на PostgreSQL | быстрый прогон без инфраструктуры; миграции проверяются на обоих |
| Новинки — отдельная вкладка, а не только флаг в форме фильма | ТЗ 4.3 требует управления статусом как отдельной задачей |
| Цена — отдельная вкладка | ТЗ 4.5 требует видеть цену, валюту и статус в одном месте |
| Мелодии ограничены и на сервере, и в UI | сервер даёт 409, интерфейс не показывает форму на четвёртой |
| Валюта через `setCurrency()`, а не через параметр `formatMoney` | не пришлось трогать десятки мест вызова |
| Узбекские месяцы прописаны вручную | `Intl` для `uz-Latn` в Chrome не даёт коротких форм |
| Фильтр по дате — параметр API, а не фильтрация на клиенте | клиент не знает, в какие дни идёт фильм |
| Username бота — поле настроек, а не переменная окружения | ссылку для шаринга меняет админ, а не деплой |
| `schedule` отдельно от `sessions` | плоский список времён нужен каталогу, сгруппированный — карточке |
| Поиск сопоставляется в Python, а не в SQL | SQLite не сворачивает регистр кириллицы, каталог мал |
| Поиск на сервере, жанр — на клиенте | поиск покрыт тестами, жанр мгновенно фильтрует уже полученное |
| Формат deep link — один модуль на бэкенде | им пользуются API, бот и Mini App; разъезд форматов исключён |
| `ApiError` со статусом | 404 по устаревшей ссылке нужно показать иначе, чем сбой сети |
| Движок закрывается после каждого теста | иначе пул переживает смену event loop и набор становится флаки |
| Формат deep-link payload — один модуль на бэкенде | кнопка, бот и Mini App читают один контракт |
| Тестовая БД — файл на процесс | общий файл ломал параллельные прогоны |
| Проверка прошедших дат в `ensure_bookable_slot` | одна точка закрывает места, удержание и подтверждение |
| Экран дат читает расписание фильма, а не окно настроек | иначе часть дат ведёт в тупик |
| Арбитр гонки — уникальный индекс, а не проверка перед вставкой | `.with_for_update()` на SQLite не блокирует |
| Строка настроек создаётся в отдельной сессии | проигрыш в гонке не должен ломать транзакцию запроса |
| Удержание берётся при выборе мест, а не при отправке | иначе `hold_minutes` — мёртвая настройка |
| Смена статуса перепроверяет места | возврат отменённой заявки и перенос на другое время — это решения о бронировании, а не флаги |
| Часовой пояс — смещение в минутах, а не зона IANA | нет `tzdata`, а в Узбекистане нет перевода часов |
| Конец сеанса = начало + длительность фильма | нельзя оценивать фильм, который ещё идёт |
| Ссылка на карту: `map_url` → координаты → адрес | кнопка работает при любом уровне заполненности настроек |
| Тёмная палитра не следует светлой теме телефона | кинотеатр в белом — это не бренд; за Telegram следует обрамление |
| Вызовы Telegram SDK обёрнуты в try | старые клиенты бросают на неизвестных методах |
| `OverflowError` → 404 обработчиком, а не ограничением каждого параметра | одна точка закрывает все маршруты, включая будущие |
| Проверки конфигурации предупреждают, но не падают | неверная настройка не должна останавливать продажу билетов |
| Валюта загружается один раз на уровне приложения | загрузка по экранам давала разные значения на разных страницах |
| Контакты брони заполняют пустой профиль | вторая бронь — в одно касание; занятые поля не трогаются |
| «Сегодня» считается по часам кинотеатра, а не сервера | сервер в UTC предлагал сеансы, которые в Ташкенте давно прошли |
| Начавшийся сеанс не предлагается и не бронируется | заявку ещё нужно подтвердить по телефону — после начала это бессмысленно |
| Ответы API остаются английскими, переводит интерфейс | у API один словарь для всех клиентов, говорит с людьми только интерфейс |

---

## 6. Переменные окружения

Секреты и инфраструктура — только здесь. Данные кинотеатра сюда **не** попадают.

| Переменная | Обязательна | Назначение |
|---|---|---|
| `BOT_TOKEN` | да | токен бота от BotFather |
| `WEBAPP_URL` | да | публичный HTTPS-адрес Mini App |
| `JWT_SECRET` | да | подпись JWT |
| `ADMIN_TELEGRAM_IDS` | да | Telegram ID администраторов через запятую |
| `DATABASE_URL` | нет | пусто → SQLite `nova-dev.db` |
| `POSTGRES_DB` / `POSTGRES_USER` / `POSTGRES_PASSWORD` | для Docker | параметры БД в compose |
| `CORS_ORIGINS` | нет | список origin через запятую, хвостовой слэш срезается |
| `TELEGRAM_AUTH_MAX_AGE_SECONDS` | нет | возраст initData, по умолчанию 86400 |
| `UPLOAD_DIR` | нет | каталог загрузок, по умолчанию `uploads` |
| `AUTO_CREATE_SCHEMA` | нет | `true` создаёт схему и сидит демо-данные в обход миграций |
| `TMDB_API_KEY` | нет | автозаполнение карточки фильма |
| `OMDB_API_KEY` | нет | рейтинг IMDb при автозаполнении |
| `TRANSLATION_API_URL` | нет | перевод RU↔UZ, по умолчанию публичный LibreTranslate |

**Требует внимания:** `CORS_ORIGINS` сейчас указывает на временный cloudflare-туннель.
Постоянный HTTPS-домен ещё не выдан — до этого адрес придётся править при каждом запуске.

---

## 7. Как запустить и проверить

```bash
python -m alembic upgrade head
python -m uvicorn app:app --host 127.0.0.1 --port 8000
```

```bash
python -m pytest -q
```

```bash
cd frontend && npm run lint -- --max-warnings=0 && npm run build
```

Состояние проверок на момент обновления: **211 тестов зелёные, tsc и eslint без ошибок,
production-сборка проходит, цепочка миграций проходит вверх, вниз до базы и снова вверх.**

---

## 8. Состояние по этапам ТЗ

Все двадцать этапов пройдены, кроме двух исключений:

| Этап | Состояние |
|---|---|
| 1–12, 14, 16–20 | выполнены |
| 13 личный кабинет | у Codex. Во время этапа 20 его правки появились в рабочем дереве: статусы `contacting` и `watched` получили подписи, билеты разделены на активные и историю. Вошли в коммит этапа 20, потому что лежат в тех же файлах; отдельно мной не проверялись |
| 15 AI-помощник | **исключён из ТЗ** решением владельца |

Что нужно от владельца перед запуском — раздел 9.

## 9. Открытые вопросы

| Вопрос | Статус |
|---|---|
| Постоянный HTTPS-домен | ожидается, до этого `CORS_ORIGINS`/`WEBAPP_URL` правятся вручную |
| Фото интерьера, зала и бара для галереи | ожидаются; раздел готов, галерея пуста и показывает «Фотографии скоро появятся». Загружаются в админке: Галерея |
| Username бота Nova Cinema | нужно указать в админке (Кинотеатр → Username бота), иначе «Поделиться» не построит ссылку |
| Промокоды из удалённой legacy-БД | сохранены: `NOVA10` 10%, `WELCOME` 15%, `NOVASINEMA` 5% — можно завести бонусами в этапе 4 |
| `BOT_TOKEN` | **заполнен и проверен**: токен рабочий, бот `@NOVA_CINEMA_searchbot`. В отчёте этапа 20 было сказано, что он пуст — это неверно, ошибка проверки |
| `WEBAPP_URL` | указывает на `https://nova-cinema-v2.vercel.app/`, адрес отвечает 500. Это старый деплой, текущий фронтенд там не живёт |
| `CORS_ORIGINS` | указывает на туннель `stat-volunteers-continued-compromise.trycloudflare.com`, он больше не резолвится |
| `POSTGRES_PASSWORD` пуст | нужен только для `docker compose`; локально проект работает на SQLite |
| `DATABASE_URL` | `sqlite+aiosqlite:///./nova-dev.db`, база мигрирована до head |
| Бонусы и мелодии | раздел «О нас» их показывает, но в базе их нет — заполняются в админке |
