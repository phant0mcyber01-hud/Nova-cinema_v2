[alembic]
script_location = alembic
# The address is read from DATABASE_URL in .env by alembic/env.py; this key
# is left empty on purpose so a stale value here cannot point migrations at
# the wrong database.
sqlalchemy.url =
